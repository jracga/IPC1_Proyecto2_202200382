// 
import * as peticion from './Conexión/comunicacion.back.js' 

export default{
    ... peticion //"..." es un operador de propagación que permite a una expresión ser expandida en situaciones donde se esperan múltiples argumentos
}